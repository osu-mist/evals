CREATE TABLE review_cycle_options (
    ID              Numeric(11) NOT NULL PRIMARY KEY,
    NAME            VARCHAR(75) NOT NULL,
    VALUE           Numeric(2) NOT NULL,
    SEQUENCE        Numeric(2) NOT NULL,
    CREATOR_PIDM    Numeric(11) NOT NULL,
    CREATE_DATE     DATE NOT NULL,
    DELETER_PIDM    Numeric(11),
    DELETE_DATE     DATE
);

CREATE TABLE ratings (
    ID NUMERIC(11) NOT NULL,
    RATE NUMERIC(3) NOT NULL,
    NAME VARCHAR(64),
    DESCRIPTION VARCHAR(512),
    APPOINTMENT_TYPE VARCHAR(45)
);

CREATE TABLE notices (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    ANCESTOR_ID INT,
    NAME VARCHAR(55),
    TEXT VARCHAR(55),
    CREATE_DATE DATE,
    CREATOR_PIDM INT
);

CREATE TABLE pyvpdlw (
    ID INT PRIMARY KEY AUTO_INCREMENT
);

CREATE TABLE pyvpdes (
    ID INT PRIMARY KEY AUTO_INCREMENT
);

CREATE TABLE salaries (
    ID INT PRIMARY KEY AUTO_INCREMENT
);

ALTER TABLE jobs
    ADD PYVPASJ_INCLUDE_RANKED_FLAG BOOLEAN,
    ADD PYVPASJ_LOW INT,
    ADD PYVPASJ_MIDPOINT INT,
    ADD PYVPASJ_HIGH INT,
    ADD PYVPASJ_SALARY FLOAT,
    ADD PYVPASJ_SGRP_CODE INT
;

ALTER TABLE permission_rules
    ADD APPROVED_GOALS VARCHAR(256),
    ADD APPOINTMENT_TYPE VARCHAR(55)
;